#include <bits/stdc++.h>
#define MX 111111
using namespace std;
int p[MX];
int main()
{
    int n, ans;
    scanf("%d", &n);
    for (int i = 1; i <= n; ++i)
        scanf("%d", &p[i]);
    for(int i=0)
    printf("%d", ans);
    return 0;
}