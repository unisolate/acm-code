#include <bits/stdc++.h>
using namespace std;
int main()
{
	int t;
	scanf("%d",&t);
	for(int cas=1;cas<=t;++cas)
	{
		scanf("%s%d",s,&b);
		for(int i=0;i<26;++i)
		{
			scanf("%d",&q);
			for(int j=0;j<q;++j)
				scanf("%d%d");
		}
		printf("Case #%d: %d\n",cas,);
	}
	return 0;
}