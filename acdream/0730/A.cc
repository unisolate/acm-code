#include <cstdio>
#define MX 100010
using namespace std;
int p[MX];
int main() {
    int t, op, x, n, m;
    scanf("%d", &t);
    while (t--) {
        scanf("%d%d", &n, &m);
        for (int i = 0; i < n; ++i)
            scanf("%d", &p[i]);
        while (m--) {
            scanf("%d%d", &op, &x);
            if (op = 1) {
                printf("%d\n", num);
            } else {
                printf("%d\n", num);
            }
        }
    }
    return 0;
}
