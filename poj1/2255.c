#include<stdio.h>
#include<string.h>
void recovertree(char pre[],char in[])
{
	int i=0;
	while(in[i]!=pre[0])
		i++;
	int prelength=i,inlength=pre.length()-i-1;
	
}
int main()
{
	char pre[30],in[30],post[30];
	while((scanf("%s %s"),pre,in)!=EOF)
	{
		pre[]
	}
	return 0;
}