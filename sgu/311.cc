#include <cstdio>
using namespace std;
int main()
{
    char o[9];
    while (~scanf("%s%d%d", o, &n, &c))
    {
        if (o[0] == 'A')
        {
            U(c, n);
        }
        else
        {
        }
    }
    return 0;
}