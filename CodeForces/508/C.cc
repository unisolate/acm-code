#include <cstdio>
using namespace std;
int w[333];
int main() {
    int m, t, r, p = -1;
    scanf("%d%d%d", &m, &t, &r);
    for (int i = 0; i < m; ++i) {
        scanf("%d", &w[i]);
    }
    if(t>=r){

    }
    printf("%d", p);
    return 0;
}