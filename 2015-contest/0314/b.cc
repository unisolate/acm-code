#include <cstdio>
using namespace std;
int n, m, d, c;
int main() {
    while (~scanf("%d%d", &n, &m)) {
        while (m--) {
            scanf("%d%d", &d, &c);
        }
        
    }
    return 0;
}