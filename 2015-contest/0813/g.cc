#include <cstdio>
#include <queue>
using namespace std;
int main() {
    int t, n, m, x, y;
    char o[9];
    scanf("%d", &t);
    while (t--) {
        scanf("%d%d", &n, &m);
        for (int i = 0; i < n; ++i) {
            for (int j = 0; j < n; ++j) {

            }
        }
        for (int i = 0; i < n; ++i) {
            for (int j = 0; j < n; ++j) {

            }
        }
        for (int i = 0; i < m; ++i) {
            scanf("%s%d%d", o, &x, &y);
        }
    }
    return 0;
}