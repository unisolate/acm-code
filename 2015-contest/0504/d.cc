#include <cstdio>
#include <map>
#include <algorithm>
using namespace std;
map<pair<pair<int, int>, pair<int, int> >, int> mp;
int main() {
    int t;
    scanf("%d", &t);
    while (t--) {
        scanf("%d%d", &n, &m);
        for (int i = 0; i < n; ++i)
            for (int j = 0; j < m; ++j)
                scanf("%d",);
        scanf("%d", &k);
        for (int i = 0; i < k; ++i)
            scanf("%d%d", &);
    }
    return 0;
}