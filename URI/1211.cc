#include <bits/stdc++.h>
using namespace std;
char s[222];
int main()
{
    while (~scanf("%d", &n))
    {
        int a = 0;
        while (n--)
        {
            scanf("%s", s);
            
        }
        printf("%d\n", a);
    }
    return 0;
}