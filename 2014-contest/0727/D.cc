#include <bits/stdc++.h>
using namespace std;
int p[111][111];
int main()
{
    int n, m;
    scanf("%d%d", &n, &m);
    for (int i = 0; i < n; ++i)
        for (int j = 0; j < m; ++j)
            scanf("%d", &p[i][j]);
    
    printf("%d\n", s);
    return 0;
}