#include <bits/stdc++.h>
using namespace std;
int main()
{
    while (~scanf("%d%d%d", &n, &m, &q))
    {
        for (int i = 0; i < n; ++i)
            scanf("%d", &a[i]);
        for (int i = 0; i < m; ++i)
            scanf("%d", &gs[i]);
        while (q--)
        {
            scanf("%d%d%d", &l, &r, &g);
            
        }
    }
    return 0;
}