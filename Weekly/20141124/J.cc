#include <cstdio>
using namespace std;
typedef long long ll;
int main()
{
    int r, y, b;
    while (~scanf("%d%d%d", &r, &y, &b))
    {
        ll ans = 0;
        
        printf("%lld\n", ans);
    }
    return 0;
}