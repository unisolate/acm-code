#include <stdio.h>
#include <string.h>
#define MAXN 101
int dp[MAXN][MAXN];
int main()
{
	int t,i,j,cas;
	scanf("%d",&t);
	for(cas=1;cas<=t;++cas)
	{
		scanf("%f%d",);
		printf("Case %d: %d\n",cas,f[v]);
	}
	return 0;
}