#include <cstdio>
using namespace std;
int main() {
    int t, cas = 0;
    scanf("%d", &t);
    while (t--) {
        int ans = 0, n;
        scanf("%d", &n);
        for (int i = 0; i < n; ++i)
            scanf("%d",);
        printf("Case $%d: %d\n", ++cas, ans);
    }
    return 0;
}